import Vue from 'vue'
import Vuex from 'vuex'
import AppConfig from './modules/app-config'

Vue.use(Vuex)

export default new Vuex.Store({
  state: {
  },
  mutations: {
  },
  actions: {
  },
  modules: {
    AppConfig
  }
})
